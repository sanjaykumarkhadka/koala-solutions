{
  "buildCommand": "pnpm --filter @koala/shared build && pnpm --filter @koala/client build",
  "outputDirectory": "packages/client/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
